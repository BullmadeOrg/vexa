- **Database schema checks no longer report false drift on Python 3.9.** Empty-lambda formatting is
  now canonical across supported Python versions, while real model changes still fail the seal gate.
