# handler/[...stack]

Catch-all page for Stack Auth's built-in sign-in and account screens. `StackHandler` selects the
screen from the remaining URL segments and renders it as a full-page flow inside the provider from
the parent layout.
